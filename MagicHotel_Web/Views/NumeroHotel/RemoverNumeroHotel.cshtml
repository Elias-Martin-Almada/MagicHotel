@model MagicHotel_Web.Models.ViewModel.NumeroHotelDeleteViewModel

@{
    ViewData["Title"] = "RemoverNumeroHotel";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<form method="post" asp-action="RemoverNumeroHotel">
    <div class="container border p-3">
        <div asp-validation-summary="All" class="text-danger"></div>
        <input asp-for="NumeroHotel.HotelNo" hidden />


        <div class="row">
            <h2 class="text-info">Remover Numero de Hotel</h2>
        </div>

        <div class="row mt-2">
            <div class="col-md-6">
                <label class="form-label">Numero de Hotel</label>
                <input asp-for="NumeroHotel.HotelNo" disabled class="form-control" />
                <span asp-validation-for="NumeroHotel.HotelNo" class="text-danger"></span>
            </div>
        </div>

        <div class="row mt-2">
            <div class="col-md-6">
                <label class="form-label">Hotel</label>
                <select asp-for="NumeroHotel.HotelId" disabled class="form-select" asp-items="@Model.HotelList">
                    <option value="">-- Seleccione un Hotel --</option>
                </select>
                <span asp-validation-for="NumeroHotel.HotelId" class="text-danger"></span>
            </div>
        </div>

        <div class="row mt-2">
            <div class="col-md-6">
                <label class="form-label">Detalle Especial</label>
                <textarea asp-for="NumeroHotel.DetalleEspecial" disabled class="form-control"></textarea>
                <span asp-validation-for="NumeroHotel.DetalleEspecial" class="text-danger"></span>

            </div>
        </div>

        <div class="d-grid gap-2 d-md-block mt-2">
            <button type="submit" class="btn btn-danger">Remover</button>
            <a asp-controller="NumeroHotel" asp-action="IndexNumeroHotel" class="btn btn-success">Regresar</a>
        </div>

    </div>
</form>

